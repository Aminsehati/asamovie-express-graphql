const {
    category
} = require('./category.model');
const {
    country
} = require('./country.model');
const {
    user
} = require('./user.model')
const {
    language
} = require('./language.model')
module.exports = {
    category,
    country,
    language,
    user
}